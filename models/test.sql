WITH JSON_SONGS_DATA AS (

  SELECT * 
  
  FROM {{ source('TESTING.PUBLIC', 'JSON_SONGS_DATA') }}

),

COMPLEXTABLE1 AS (

  SELECT * 
  
  FROM {{ source('TESTING.PUBLIC', 'COMPLEXTABLE1') }}

),

Reformat_2 AS (

  SELECT * 
  
  FROM COMPLEXTABLE1 AS in0

),

Reformat_1 AS (

  SELECT * 
  
  FROM JSON_SONGS_DATA AS DEMONSTRATION4

)

SELECT * 

FROM Reformat_1
