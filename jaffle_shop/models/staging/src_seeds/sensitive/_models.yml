version: 2

models:
  - name: stg_customers
    meta:
      owner: "maja.cernja@krakentechnologies.ltd"
    description: List of jaffle shop customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: first_name
        meta:
          sensitive: true
      - name: last_name
        meta:
          sensitive: true
